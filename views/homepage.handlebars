<section>
{{#each posts as |post|}}
<article class="card">
    <a href="/post/{{post.id}}">
        <header>{{post.title}}</header>
        <p>{{post.content}}</p>
    </a>
</article>
{{/each}}
</section>